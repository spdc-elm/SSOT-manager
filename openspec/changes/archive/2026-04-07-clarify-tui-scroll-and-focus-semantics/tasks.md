## 1. Spec Clarification

- [x] 1.1 Update the `terminal-ui-shell` spec to define staged `h` behavior while detail focus is active
- [x] 1.2 Update the `terminal-ui-shell` spec to require a visible scrollbar or scrollbar-like overflow affordance for long inspection detail content
- [x] 1.3 Update the `profile-config-editing` spec to require visible overflow affordances for long collection editor popups

## 2. Consistency Verification

- [x] 2.1 Verify the current TUI implementation already matches the clarified inspection and editing behaviors
- [x] 2.2 Verify README guidance remains consistent with the clarified inspect-mode `h` semantics and visible overflow affordances
